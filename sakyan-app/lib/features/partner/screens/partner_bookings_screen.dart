import 'package:flutter/material.dart';

class PartnerBookingsScreen extends StatelessWidget {
  const PartnerBookingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PartnerBookingsScreen')),
      body: const Center(child: Text('PartnerBookingsScreen — coming soon')),
    );
  }
}
