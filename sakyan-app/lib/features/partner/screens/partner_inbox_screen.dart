import 'package:flutter/material.dart';

class PartnerInboxScreen extends StatelessWidget {
  const PartnerInboxScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PartnerInboxScreen')),
      body: const Center(child: Text('PartnerInboxScreen — coming soon')),
    );
  }
}
