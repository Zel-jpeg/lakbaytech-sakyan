import 'package:flutter/material.dart';

class Step1TypeScreen extends StatelessWidget {
  const Step1TypeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Step1TypeScreen')),
      body: const Center(child: Text('Step1TypeScreen — coming soon')),
    );
  }
}
