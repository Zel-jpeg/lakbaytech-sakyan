import 'package:flutter/material.dart';

class Step2InfoScreen extends StatelessWidget {
  const Step2InfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Step2InfoScreen')),
      body: const Center(child: Text('Step2InfoScreen — coming soon')),
    );
  }
}
