import 'package:flutter/material.dart';

class Step3DocsScreen extends StatelessWidget {
  const Step3DocsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Step3DocsScreen')),
      body: const Center(child: Text('Step3DocsScreen — coming soon')),
    );
  }
}
