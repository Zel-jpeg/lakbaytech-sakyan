import 'package:flutter/material.dart';

class Step4PendingScreen extends StatelessWidget {
  const Step4PendingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Step4PendingScreen')),
      body: const Center(child: Text('Step4PendingScreen — coming soon')),
    );
  }
}
