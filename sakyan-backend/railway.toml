[build]
builder = "nixpacks"
buildCommand = "python manage.py collectstatic --noinput"

[deploy]
startCommand = "python manage.py migrate && gunicorn sakyan.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
restartPolicyType = "on_failure"